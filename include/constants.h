#define PTRBLOCKSIZE 64

#include <vector>

#include "information.h"

// std::vector<std::array<double*>(PTRBLOCKSIZE)>* wavefunctions = std::vector<std::vector<>*>;
// wavefunctions.reserve(PTRBLOCKSIZE);